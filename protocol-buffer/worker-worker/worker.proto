syntax = "proto3";
package workerpb;

message Worker {
    uint64 fromVertex = 1; // sender ID
    uint64 stepcount = 2;
    uint64 toVertex= 3;
    double msgValue = 4;
}

message WorkerTotal{
    repeated Worker vertices = 1;
}
